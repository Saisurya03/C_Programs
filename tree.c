#include<stdio.h>
#include<conio.h>

struct node
 {
    int d;
    int *r;
    int *l;
 }*root;

 struct node *insert(int,int *)
 {
    int *ptr;
    ptr=(struct node*)malloc(sizeof(struct node))
    if(root==NULL)
    {
        p->d=
    }
 };
